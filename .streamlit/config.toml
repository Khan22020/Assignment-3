[server]
headless = true
address = "0.0.0.0"
port = 5000

[theme]
primaryColor = "#b80e0e"  # Witcher red
backgroundColor = "#1a1a1a"  # Dark background
secondaryBackgroundColor = "#2d2d2d"  # Slightly lighter dark
textColor = "#ffffff"  # White text
font = "sans serif"

[browser]
gatherUsageStats = false
